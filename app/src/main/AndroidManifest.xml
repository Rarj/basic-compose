<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.arj.practicecomposebasic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PracticeComposeBasic">
        <activity
            android:name=".quadrant.QuadrantActivity"
            android:exported="false"
            android:label="@string/title_activity_quadrant" />
        <activity
            android:name=".taskmanager.TaskManagerActivity"
            android:exported="false"
            android:label="@string/title_activity_task_manager" />
        <activity
            android:name=".article.ArticleActivity"
            android:exported="false"
            android:label="@string/title_activity_article" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>